# Code Review · 2026-04-18 (первое)

> **Периодичность**: раз в 2 месяца. Следующее: 2026-06-18.
> **Фокус**: полный скан (backend ETL · frontend · data integrity · docs).
> **Инструмент**: `python etl/audit_tools.py` + ручной скан.

## Резюме

| Severity | Кол-во | Что делаем |
|---|---:|---|
| 🔴 P0 | **2** | Чиню в этом же ревью |
| 🟡 P1 | **5** | → `05_BACKLOG.md` с effort-оценкой |
| 🔵 P2 | **6** | → `07_ANTI_PATTERNS.md` или живём с ними |
| ✅ OK | **7** | Отмечено как проверенное |

Общий вывод: **серьёзных архитектурных проблем нет**. Основной тех. долг — копипаст JS-функций между страницами (renderCancel* после переноса) и устаревшие cache-busters на страницах которые редко правим.

---

## 🔴 P0 (чиню сейчас)

### P0-1. `data.js` cache-buster разнобой: 6 разных версий на 13 страницах

**Что:** В коде я добавил `V7.waHref()`, новые helpers, но на 8 страницах стоит `data.js?v=20260414f` — они не подхватят обновление.

**Факты:**
```
v=20260414f   — 8 страниц (старые, включая finance, management, team, recall)
v=20260414k   — 1 (старая)
v=20260416m   — 1
v=20260417cancel — clients.html
v=20260417r   — revenue.html
v=20260417t   — dashboard.html
```

Страницы без cache-buster на `theme.css`: 30 штук (ушли только dashboard, clients, revenue).

**Последствие:** Страницы `recall.html`, `finance.html`, `management.html`, `club-members.html`, `tournament-*` могут падать при вызове `V7.waHref()` или других новых helpers — на телефоне у пользователя в кеше старый `data.js`.

**Фикс:** массово обновить на единую версию `v=20260418` в `<script src="assets/data.js?v=...">` и `<link ...theme.css?v=...>` во всех 37 HTML-файлах.

### P0-2. `dashboard.html` — 183 строки мёртвого закомментированного кода

**Что:** Когда я переносил render-функции отмен на `/clients`, я обернул блок `/* BEGIN DEPRECATED ... END DEPRECATED */` вместо удаления. 7.9 KB закомментированного JS висит в проде, скачивается каждым пользователем, индексируется поисковиками.

**Фикс:** удалить блок. Функции уже в `/clients.html`, откатывать будет через git если понадобится.

---

## 🟡 P1 (в `05_BACKLOG.md`)

### P1-1. 4 manager-*.html — заглушки WIP

**Файлы:** `manager-kpi.html`, `manager-tasks.html`, `manager-checklists.html`, `manager-schedule.html` — по ~3 KB каждый, содержат только заглушку «Скоро/WIP».

**Вопрос бизнеса:** роль manager вообще нужна? Есть admin (офис), coach (тренеры), manager (…?). Если нет — удалить. Если есть — приоритизировать наполнение.

Effort: нужно решение (1 мин) + контент (2-6 часов если делаем).

### P1-2. `scrape_debts.py` не в cron

**Что:** Есть полноценный скрипт для debts + balance_log (`--full` / `--update`), но он не вызывается из `run_all_etl.py`. То есть таблицы `debts` и `balance_log` не обновляются автоматически.

**Вопрос:** используются ли на сайте? Быстрый grep покажет.

Effort: 10 мин (добавить в cron если нужно, либо явно помечать как ad-hoc в docstring).

### P1-3. Дубли render-функций: `renderCancel*` в `dashboard.html` И `clients.html`

**Что:** После переноса отмен на `/clients` я оставил функции в `dashboard.html` обёрнутыми в `/* DEPRECATED */`. Парсер audit_tools правильно видит их как объявления. Дубль кода.

**Совмещается с P0-2** (удаление deprecated блока).

После P0-2 дубли исчезнут → P1-3 закроется.

### P1-4. Orphan cache tab `rev_weekly`

**Что:** Таб `rev_weekly` пишется в `build_cache.py`, но никто его не читает на фронте. Revenue.html использует `rev_weekly_income` и `rev_weekly_goods` (разделённые). `rev_weekly` = `income + goods` = избыточный.

**Фикс:** убрать запись `rev_weekly` из `build_cache.py::main()`.

Effort: 5 мин + удалить таб из `v7padel_cache` через service account.

### P1-5. `revenue.html` — 179 KB, самый тяжёлый HTML после `intensive.html`

**Что:** `revenue.html` стал большим (179 KB, 3500+ строк inline JS). Всё работает, но на мобилке первая загрузка — 179 KB + Chart.js + data.js. Плюс много дублирующейся логики между табами (income / goods / margins / utilization).

**Фикс (в будущем):** вынести общие helpers в отдельный `revenue-helpers.js`. Сейчас не P0 — работает.

Effort: 2-3 часа рефакторинга.

---

## 🔵 P2 (в `07_ANTI_PATTERNS.md` или живём)

### P2-1. `compute_cancel_top_clients` — deprecated но не удалён

**Что:** Функция в `build_cache.py` не вызывается из `main()` (я её сам пометил как deprecated после отказа от ТОП-20 отмен, 28% покрытие). Занимает ~100 строк. Оставлена на случай «вернуть когда будет snapshot-based tracking».

**Решение:** оставляем с явной пометкой `# DEPRECATED — see 05_BACKLOG snapshot tracking` в docstring. Если через 2 ревью (через 4 месяца) не вернётся — удалить.

### P2-2. `intensive.html` — 2 МB HTML-файла

**Что:** Размер файла нереальный для HTML. Скорее всего base64-embedded изображения или data. Открывать не буду без необходимости — не трогал. Но заслуживает отдельного пассы: проверить что это и не мешает ли.

Effort: 30 мин исследование.

### P2-3. TODO/FIXME без даты — 17 находок

**Что:** В `generate_state_doc.py` есть 6 TODO про «кэш на счетах», «burn rate», «runway» — ждут от тебя данных. Остальное — self-references в `audit_tools.py` (мои собственные доки) и пара комментариев в генерированном коде.

**Решение:** добавить даты к TODO в `generate_state_doc.py`. Если >3 мес. TODO без движения — делать или удалять.

### P2-4. 71 UI-функция без `try/catch`

**Что:** audit_tools нашла 71 render-функцию >15 строк без внешнего try/catch. Реалистично — нельзя обернуть всё. **Правило #10 (фиксированное после /revenue ReferenceError)**: UI-функции критически важного маркетинга (KPI-карточки, графики) оборачивать — это делаем. Служебные render (таблицы, списки) можно без.

**Решение:** живём. Правило уже есть в 12_WORKING_WITH_CLAUDE.md.

### P2-5. `log.flush()` в ETL — проверить все sync_* скрипты

**Что:** В `run_all_etl.py` теперь buffering=1. Но отдельные `sync_*.py` используют `logging` — там буферизация нормальная (logging всегда flush на каждый record).

**Статус:** OK, уже покрыто. Пометка.

### P2-6. `theme.css` cache-buster только на 3 страницах из 33

**Что:** Когда я правил mobile-responsive в `theme.css` → поставил cache-buster только на dashboard/clients/revenue. Остальные 30 страниц показывают ту же версию `theme.css` без буста.

**Влияние:** на старых страницах mobile-improvements (horizontal scroll таблиц) **уже работают** (потому что браузер всё равно подтянет свежую версию через ETag в течение суток), но если у пользователя агрессивный кеш — старая версия будет до перезагрузки.

**Решение:** синхронизируем cache-buster в P0-1 (массовое обновление всех страниц) → заодно закроется.

---

## ✅ OK (проверено)

1. **Secrets в коде** — нет хардкод-паролей/токенов (grep прошёл).
2. **Stale sync-логи** — все sync'и отработали в последние 7 дней.
3. **`04_CURRENT_STATE.md`** — сгенерирован сегодня (7:40). Свежий.
4. **Cron structure** — все sync_*.py (кроме ad-hoc `scrape_debts`) включены после сегодняшнего фикса.
5. **Dedup rules (top-up + manual_entries)** — реально работают, проверено сегодня утром.
6. **JS balance braces/brackets** — все страницы в норме (minor false-positives от regex литералов).
7. **Documentation** — все ADR на месте, `12_WORKING_WITH_CLAUDE.md` и `13_ACCOUNTING_RULES.md` свежие.

---

## План действий

### Сейчас (в этом же сеансе):
- [x] P0-1: массовое обновление cache-busters на всех HTML → `v=20260418`.
- [x] P0-2: удалить `/* DEPRECATED */` блок из `dashboard.html`.
- [x] P1-4: убрать orphan `rev_weekly` таб.

### В `05_BACKLOG.md` (пользователь решает):
- [ ] P1-1: решение по `manager-*.html` (удалить/наполнить).
- [ ] P1-2: `scrape_debts` в cron или явное removing.
- [ ] P1-5: рефакторинг `revenue.html` на отдельный helpers.js.

### Через 2 ревью (4 месяца):
- P2-1: `compute_cancel_top_clients` — удалить если не вернулся.

---

## Следующий запуск

- **Дата**: 2026-06-18.
- **Команда**: `python etl/audit_tools.py` → собрать metrics → заполнить `REVIEW_2026-06-18.md`.
- **Ожидается**: сравнить с этим baseline, посмотреть появились ли новые P0/P1.
- **Метрика процесса**: число P0 должно падать (через 2-3 цикла — идеально 0 P0 на входе).
